{
  "name": "ksa-bridge",
  "private": true,
  "description": "Maintainer-side npm dependency tree for vendoring minified libs into examples/*/lib/. Not a runtime dependency of the mod or the consoles.",
  "scripts": {
    "vendor": "node scripts/vendor.js"
  },
  "dependencies": {
    "mqtt": "4.3.7",
    "topojson-client": "^3.1.0"
  }
}
